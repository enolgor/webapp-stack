<webapp-configuration>
	<database>
		<url>jdbc:h2:${default.sys.test.application.data.path}/${project.artifactId}</url>
		<username>root</username>
		<password>root</password>
	</database>
</webapp-configuration>